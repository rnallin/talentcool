{
  "buildCommand": "cd artifacts/hr-platform && PORT=5173 BASE_PATH=/ npx vite build",
  "outputDirectory": "artifacts/hr-platform/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
